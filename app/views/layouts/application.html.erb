<!DOCTYPE html>
<html>
  <head>
    <title>Event</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>


    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/bulma/0.7.4/css/bulma.min.css">

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <nav class="navbar is-info is-fixed-top", role="navigation">
      <div id="event-navbar" class="navbar-menu">
        <% if  current_user%>
        <div class="navbar-start">
            <%= link_to  "My Page", user_path(current_user), class: "navbar-item" %>
            <%= link_to  "Public events", occasions_path, class: "navbar-item" %>
            <div class="navbar-item has-dropdown is-hoverable">
              <a class="navbar-link">Upcoming events</a>
              <div class="navbar-dropdown">
                <% if  current_user.occasions.count > 0%>
                  <%= link_to  current_user.occasions.first.name, current_user.occasions.first, class: "navbar-item" %>
                <% else %>
                  <p class="navbar-item">No upcoming events</p>
                <% end %>
                <% if  current_user.occasions.count > 1%>
                  <%= link_to  current_user.occasions.second.name, current_user.occasions.second, class: "navbar-item" %>
                <% end %>
                <% if  current_user.occasions.count > 2%>
                  <%= link_to  current_user.occasions.third.name, current_user.occasions.third, class: "navbar-item" %>
                <% end %>
              </div>
            </div>
          </div>
          <% else %>
            <div class="welcome"></div>
              <p class="word"><strong>Welcome to Main Event</strong></p>
            <% end %>
          <div class="navbar-end">
            <div class="navbar-item">
              <div class="buttons">
                <% if current_user %>
                <%= link_to  current_user.name, user_path(current_user), class: "button is-link" %>
                <%= link_to  "Log out", logout_path, class: "button is-primary" %>
                <% end %>
              </div>
            </div>
          </div>
        </div>
    </nav>
    <div class="padding">
      <br>
      <br>
      <br>
    </div>
    <%= yield %>

    <div class="footer" style="z-index:-5">
      <p><strong>Main Event</strong> by <strong>Khang Le</strong> and <strong>Ali Madni</strong></p>
      <p>Made with Ruby on Rails</p>
    </div>
  </body>
</html>
